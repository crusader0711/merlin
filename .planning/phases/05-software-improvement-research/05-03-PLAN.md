---
phase: 05-software-improvement-research
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - 04_research/04_fpga_optimization.md
  - 04_research/05_ml_detection.md
autonomous: true
requirements: [SWRES-04, SWRES-05]

must_haves:
  truths:
    - "FPGA pipeline optimization research covers HLS vs hand-coded Verilog, loop unrolling, and multi-bank memory with resource margin analysis against documented XC7A100T utilization"
    - "ML-based detection alternatives include FPGA inference feasibility assessment with INT8 quantization analysis and DSP/BRAM estimates"
    - "FPGA optimization proposals include resource margin analysis against current ~26% LUT, ~37% DSP, ~75% BRAM utilization"
    - "ML proposals address 8-bit ADC dynamic range limitation per Open Question 3"
  artifacts:
    - path: "04_research/04_fpga_optimization.md"
      provides: "FPGA pipeline optimization research survey"
      contains: "Feasibility Assessment"
    - path: "04_research/05_ml_detection.md"
      provides: "ML-based detection alternatives survey"
      contains: "Feasibility Assessment"
  key_links:
    - from: "04_research/04_fpga_optimization.md"
      to: "02_hardware/05_fpga_board.md"
      via: "FPGA resource capacity cross-reference"
      pattern: "XC7A100T|63.400|240|135"
    - from: "04_research/04_fpga_optimization.md"
      to: "03_software/01_fpga_pipeline.md"
      via: "Pipeline module inventory cross-reference"
      pattern: "Stage"
    - from: "04_research/05_ml_detection.md"
      to: "03_software/01_fpga_pipeline.md"
      via: "Current detection baseline (Stage 9)"
      pattern: "SW-7"
---

<objective>
Write two research survey documents: FPGA pipeline optimization (SWRES-04) and ML-based detection alternatives (SWRES-05).

Purpose: Both are FPGA-centric research. SWRES-04 evaluates optimization techniques for the existing pipeline, SWRES-05 evaluates whether ML approaches can replace or augment the current threshold detector. The Artix-7 resource constraint is the central feasibility question for both.

Output: `04_research/04_fpga_optimization.md` and `04_research/05_ml_detection.md`
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-software-improvement-research/05-RESEARCH.md

@00_notation/conventions.md
@00_notation/symbol_table.md
@02_hardware/05_fpga_board.md
@03_software/01_fpga_pipeline.md
@01_physics/04_detection_theory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write FPGA pipeline optimization research survey</name>
  <files>04_research/04_fpga_optimization.md</files>
  <action>
Create the FPGA pipeline optimization research document following the mandatory 5-section structure.

Section 1 - Current State: Reference FPGA module inventory (25+ modules) from `02_hardware/05_fpga_board.md` and pipeline architecture from `03_software/01_fpga_pipeline.md`. Document current resource utilization estimates: ~26% LUT (16,500/63,400), ~37% DSP (89/240), ~75% BRAM (101/135). Note three clock domains (100/120/400 MHz). Emphasize these are theoretical estimates pending Vivado reports per Open Question 1 from research.

Section 2 - Literature Survey: Cover four optimization areas:
- Vitis HLS vs hand-coded Verilog: HLS provides ~4x faster development but 15-30% resource overhead. For resource-constrained Artix-7, hand-coded Verilog preferred for performance-critical modules (FFT, matched filter). HLS appropriate for less critical modules (USB interface, control logic). Discuss QoR (Quality of Results) comparison methodology.
- Loop unrolling for FFT/matched filter: Current 1024-pt FFT uses radix-2 DIT. Radix-4 or split-radix reduces clock cycles by ~2x but increases DSP usage by ~2x. Current DSP headroom (~152 available) supports this. Calculate throughput improvement and latency reduction.
- Multi-bank memory architecture: Current Doppler processor uses single-port BRAM. Artix-7 BRAMs are natively dual-port -- leveraging this allows simultaneous read/write, reducing processing time. Discuss bank interleaving for FFT butterfly operations.
- Pipeline parallelism: Evaluate duplicate processing paths for range bins. Current pipeline is serial (one range bin at a time). Discuss area-time tradeoffs for 2x or 4x parallelism.

CRITICAL per anti-pattern from research: Never cite GPU benchmarks as evidence of Artix-7 feasibility. All optimization estimates must be for XC7A100T specifically.

Section 3 - Gap Analysis: Current pipeline is functionally correct but not optimized for throughput. Single-port BRAM usage limits memory bandwidth. Radix-2 FFT uses more clock cycles than necessary. No HLS evaluation has been performed.

Section 4 - Feasibility Assessment: One table per optimization. Show resource delta (how much additional resource used) vs. current utilization. Flag any optimization that would push BRAM beyond 90% utilization. Include estimated throughput improvement factor.

Section 5 - Recommendations: Dual-port BRAM as Priority 1 (no additional resources, immediate throughput gain), radix-4 FFT as Priority 2 (trades DSP for speed), HLS for non-critical modules as Priority 3 (development velocity improvement).

Follow all conventions from conventions.md. Flag all estimates as "based on theoretical utilization pending Vivado reports" per Open Question 1.
  </action>
  <verify>
    <automated>test -f 04_research/04_fpga_optimization.md && grep -c "Feasibility Assessment" 04_research/04_fpga_optimization.md && grep -c "Vitis HLS" 04_research/04_fpga_optimization.md && grep -c "XC7A100T" 04_research/04_fpga_optimization.md</automated>
  </verify>
  <done>FPGA optimization document exists with HLS, loop unrolling, multi-bank memory, and parallelism analysis, resource margins computed against current utilization, all estimates flagged as theoretical</done>
</task>

<task type="auto">
  <name>Task 2: Write ML-based detection research survey</name>
  <files>04_research/05_ml_detection.md</files>
  <action>
Create the ML-based detection research document following the mandatory 5-section structure.

Section 1 - Current State: Reference current fixed threshold detector (Eq. SW-7 in `03_software/01_fpga_pipeline.md`). Cross-reference detection theory baseline (DET-17 through DET-24 in `01_physics/04_detection_theory.md`). Note Artix-7 constraints: 240 DSP48E1s, 135 BRAMs, no floating-point hardware. Note 8-bit ADC (AD9484) limits range-Doppler map dynamic range to ~49.9 dB SQNR per Open Question 3.

Section 2 - Literature Survey: Cover three ML approaches:
- Autoencoder-based detection: Small autoencoder (encoder: 64->32->16, decoder: 16->32->64) operating on range-Doppler patches. Anomaly detection by reconstruction error. Cite PMC 9370841 for MIMO FMCW autoencoder. Estimated: ~2,000-4,000 LUTs, 20-40 DSPs, 5-10 BRAMs for INT8 inference.
- CNN range-Doppler detectors: Convolutional neural networks on 2D range-Doppler maps. Discuss model size constraints: XC7A100T supports ~10K-50K INT8 parameters using available resources. >100K parameters infeasible without external memory. Cite arXiv 2509.04153 for FPGA CNN survey, PMC 10857097 for radar CNN+LSTM on FPGA.
- Hybrid approaches: Traditional CFAR + ML post-processing. CFAR runs on FPGA, ML classification runs on host PC (Python). Avoids FPGA resource constraint entirely for ML portion.

CRITICAL per Pitfall 2: Never cite GPU/cloud benchmarks as FPGA-feasible. For every ML proposal include: (a) model size in parameters, (b) INT8 quantization impact on accuracy, (c) DSP/LUT estimate for inference engine, (d) whether it fits in XC7A100T.

CRITICAL per Open Question 3: Discuss whether 8-bit quantized range-Doppler maps provide sufficient resolution for CNN/autoencoder detection. This is a key feasibility question.

Section 3 - Gap Analysis: No ML capability exists. Fixed threshold has no learning or adaptation. No labeled training data available. 8-bit ADC limits input dynamic range for ML approaches.

Section 4 - Feasibility Assessment: One table per approach. Autoencoder: MARGINAL (competes with FFT for DSPs). CNN detector: INFEASIBLE for large models, MARGINAL for tiny models (<10K params). Hybrid: FEASIBLE (ML on host PC, only CFAR on FPGA). Note training data acquisition as a non-trivial prerequisite.

Section 5 - Recommendations: Hybrid CFAR+ML as Priority 1 (feasible, leverages existing host PC), tiny autoencoder as Priority 2 (requires careful resource allocation), CNN detector as Priority 3 (research-stage, FPGA feasibility uncertain). Note that training data collection is a prerequisite for all ML approaches.

Follow all conventions from conventions.md.
  </action>
  <verify>
    <automated>test -f 04_research/05_ml_detection.md && grep -c "Feasibility Assessment" 04_research/05_ml_detection.md && grep -c "INT8" 04_research/05_ml_detection.md && grep -c "autoencoder" 04_research/05_ml_detection.md</automated>
  </verify>
  <done>ML detection document exists with autoencoder, CNN, and hybrid approaches, INT8 quantization analysis included, 8-bit ADC limitation discussed, Artix-7 feasibility verdicts assigned</done>
</task>

</tasks>

<verification>
- Both files exist in 04_research/
- Each file has all 5 mandatory sections
- FPGA optimization document includes resource margin analysis against current utilization
- ML detection document includes INT8 quantization and 8-bit ADC analysis
- No GPU/cloud benchmarks presented as FPGA-feasible per Pitfall 2
- All estimates flagged as theoretical pending Vivado reports
- No implementation specifications
</verification>

<success_criteria>
- 04_research/04_fpga_optimization.md covers HLS, loop unrolling, multi-bank memory with resource margin analysis
- 04_research/05_ml_detection.md covers autoencoder, CNN, hybrid with FPGA inference feasibility
- Both documents reference Phase 1-4 baseline with specific equations and sections
- Resource-constrained proposals (especially BRAM) properly flagged
</success_criteria>

<output>
After completion, create `.planning/phases/05-software-improvement-research/05-03-SUMMARY.md`
</output>
