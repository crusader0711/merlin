---
phase: 05-software-improvement-research
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 04_research/03_range_extension.md
  - 04_research/06_pulse_compression.md
autonomous: true
requirements: [SWRES-03, SWRES-06]

must_haves:
  truths:
    - "Coherent and non-coherent integration techniques are surveyed with expected range improvement derivations referenced to the documented noise figure chain"
    - "NLFM waveform optimization is surveyed with sidelobe reduction analysis and DAC feasibility assessment for the 8-bit/120 MHz constraint"
    - "Range migration constraints are analyzed for CPI extension proposals per Pitfall 6"
    - "Every algorithm includes an Artix-7 feasibility table"
  artifacts:
    - path: "04_research/03_range_extension.md"
      provides: "Range extension via SNR optimization survey"
      contains: "Feasibility Assessment"
    - path: "04_research/06_pulse_compression.md"
      provides: "Pulse compression improvements survey"
      contains: "Feasibility Assessment"
  key_links:
    - from: "04_research/03_range_extension.md"
      to: "01_physics/05_noise_analysis.md"
      via: "Noise figure chain cross-reference"
      pattern: "NF-1.*NF-18"
    - from: "04_research/06_pulse_compression.md"
      to: "01_physics/02_lfm_waveform_model.md"
      via: "LFM waveform theory baseline"
      pattern: "LFM-"
    - from: "04_research/06_pulse_compression.md"
      to: "03_software/01_fpga_pipeline.md"
      via: "Matched filter documentation cross-reference"
      pattern: "Stage [45]|matched filter"
---

<objective>
Write two research survey documents: range extension via SNR optimization (SWRES-03) and pulse compression improvements (SWRES-06).

Purpose: Both address improving detection range -- SWRES-03 through integration gain, SWRES-06 through better waveforms. Range extension proposals must be grounded in the documented noise figure chain, and NLFM proposals must address the 8-bit DAC constraint.

Output: `04_research/03_range_extension.md` and `04_research/06_pulse_compression.md`
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-software-improvement-research/05-RESEARCH.md

@00_notation/conventions.md
@00_notation/symbol_table.md
@01_physics/05_noise_analysis.md
@01_physics/02_lfm_waveform_model.md
@01_physics/01_fmcw_theory.md
@03_software/01_fpga_pipeline.md
@02_hardware/05_fpga_board.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write range extension research survey</name>
  <files>04_research/03_range_extension.md</files>
  <action>
Create the range extension research document following the mandatory 5-section structure.

Section 1 - Current State: Reference the noise figure chain from `01_physics/05_noise_analysis.md` (Eqs. NF-1 through NF-18) and the radar range equation from `01_physics/01_fmcw_theory.md`. Document current system parameters: 32-chirp CPI, 1024-point FFT, range resolution from FMCW-1. Cross-reference FPGA pipeline Stages 4-7 in `03_software/01_fpga_pipeline.md`.

Section 2 - Literature Survey: Cover three main approaches:
- Coherent integration: SNR improves by factor M for M coherently integrated pulses. Current M=32 chirps per CPI. Derive improvement: doubling to M=64 gives 3 dB, range extends by factor 2^(3/40) = 1.19x (range scales as SNR^0.25 per radar equation). Discuss M=64, 128, 256 with diminishing returns.
- Non-coherent integration: Magnitude-squared integration when coherent integration is limited by target motion. Gain ~ sqrt(M). Less efficient but tolerant of phase errors. Discuss post-Doppler non-coherent integration.
- Longer CPI with range migration compensation: For fast targets, longer CPI causes range migration exceeding one range bin. Calculate the critical velocity: v_critical = delta_R / T_CPI. For current system parameters, compute this explicitly. Survey Keystone transform and other range migration compensation techniques.

CRITICAL per Pitfall 6: Every CPI extension proposal MUST include range migration analysis. Calculate max unambiguous velocity for proposed CPI durations. Flag range migration compensation requirements.

Derive expected range improvement for each approach using the radar range equation, referencing the documented noise figure chain values from NF equations.

Section 3 - Gap Analysis: Current system uses only 32-chirp coherent integration. No non-coherent integration implemented. No range migration compensation. No adaptive CPI selection.

Section 4 - Feasibility Assessment: For each approach, include Artix-7 resource table. Coherent integration (longer CPI) mainly costs BRAM for additional chirp storage -- calculate BRAM requirements for M=64, 128. Non-coherent integration adds magnitude-squared accumulator. Range migration compensation (Keystone) requires BRAM for resampling buffer.

Section 5 - Recommendations: Rank by effectiveness and BRAM impact. M=64 CPI as Priority 1 (modest BRAM cost), non-coherent integration as Priority 2, range migration compensation as Priority 3 (research-stage due to BRAM constraint).

Follow all conventions from conventions.md.
  </action>
  <verify>
    <automated>test -f 04_research/03_range_extension.md && grep -c "Feasibility Assessment" 04_research/03_range_extension.md && grep -c "range migration" 04_research/03_range_extension.md && grep -c "NF-" 04_research/03_range_extension.md</automated>
  </verify>
  <done>Range extension document exists with coherent/non-coherent integration and range migration analysis, references noise figure chain, Artix-7 feasibility tables for each approach</done>
</task>

<task type="auto">
  <name>Task 2: Write pulse compression improvements research survey</name>
  <files>04_research/06_pulse_compression.md</files>
  <action>
Create the pulse compression improvements research document following the mandatory 5-section structure.

Section 1 - Current State: Reference the LFM waveform model from `01_physics/02_lfm_waveform_model.md` (LFM equations). Document current matched filter implementation from `03_software/01_fpga_pipeline.md` Stage 4-5 (overlap-save convolution). Note current LFM chirp stored in .mem files, output via 120 MHz DAC. Current sidelobes limited by rectangular window on LFM.

Section 2 - Literature Survey: Cover three approaches:
- NLFM waveform optimization: Nonlinear frequency modulation achieves -40 to -50 dB sidelobes WITHOUT the SNR loss of windowed LFM. Cite Scientific Reports 2025 paper. Methods: genetic algorithm optimization, spline-based frequency functions, piecewise-linear FM. Discuss Doppler tolerance of NLFM vs LFM.
- Windowed matched filter improvements: Alternative to NLFM -- apply window functions to the matched filter reference (Hamming, Hanning, Taylor) for sidelobe reduction. SNR loss quantified (typically 1-2 dB). Compare SNR loss vs sidelobe improvement tradeoffs.
- Stretch processing optimization: For FMCW specifically, stretch processing (dechirp) is already the baseline. Discuss potential improvements in frequency estimation (zero-padding, interpolated FFT, CZT for fine resolution).

CRITICAL per Open Question 4 from research: NLFM waveform DAC feasibility must be analyzed. The current 8-bit DAC at 120 MHz limits waveform fidelity. Calculate NLFM coefficient precision requirements and compare against DAC specs. Assess quantization effects on NLFM sidelobe performance.

Section 3 - Gap Analysis: Current system uses LFM with rectangular window. No sidelobe reduction applied. No NLFM capability. Matched filter reference is fixed (not adaptive).

Section 4 - Feasibility Assessment: For each approach, include Artix-7 resource table. NLFM: same BRAM as current LFM (same number of samples), but matched filter reference must also be updated. Windowed matched filter: minimal additional resources (window coefficients in LUT). Stretch processing: minimal changes. Flag DAC precision constraint for NLFM.

Section 5 - Recommendations: Windowed matched filter as Priority 1 (lowest risk, immediate improvement), NLFM as Priority 2 (better sidelobes but needs DAC feasibility validation), stretch processing improvements as Priority 3.

Follow all conventions from conventions.md.
  </action>
  <verify>
    <automated>test -f 04_research/06_pulse_compression.md && grep -c "Feasibility Assessment" 04_research/06_pulse_compression.md && grep -c "NLFM" 04_research/06_pulse_compression.md && grep -c "DAC" 04_research/06_pulse_compression.md</automated>
  </verify>
  <done>Pulse compression document exists with NLFM, windowed matched filter, and stretch processing analysis, DAC feasibility addressed, Artix-7 feasibility tables for each approach</done>
</task>

</tasks>

<verification>
- Both files exist in 04_research/
- Each file has all 5 mandatory sections
- Range extension document includes range migration analysis per Pitfall 6
- Pulse compression document addresses DAC precision constraint per Open Question 4
- All resource estimates stay within Artix-7 headroom or are flagged
- No implementation specifications (investigation steps only)
</verification>

<success_criteria>
- 04_research/03_range_extension.md covers coherent/non-coherent integration with range migration analysis
- 04_research/06_pulse_compression.md covers NLFM with DAC feasibility and windowed matched filter
- Both documents reference Phase 1-4 baseline documentation with specific equation numbers
- BRAM-constrained proposals flagged appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/05-software-improvement-research/05-02-SUMMARY.md`
</output>
