---
phase: 02-physics-foundation
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - 01_physics/05_noise_analysis.md
autonomous: true
requirements: [PHYS-06]

must_haves:
  truths:
    - "Friis cascaded noise figure formula is derived and applied to the full receive chain symbolically"
    - "ADC quantization noise is explicitly modeled as a noise contributor (8-bit AD9484 significance)"
    - "CIC filter noise growth is analyzed as a digital processing noise contribution"
    - "Complete system noise figure budget traces from antenna through digital output"
    - "All derivations are symbolic with pending numerical evaluation section"
  artifacts:
    - path: "01_physics/05_noise_analysis.md"
      provides: "Cascaded noise figure chain analysis from antenna to digital output"
      contains: "\\tag{NF-"
  key_links:
    - from: "01_physics/05_noise_analysis.md"
      to: "01_physics/01_fmcw_theory.md"
      via: "References radar equation SNR"
      pattern: "01_fmcw_theory\\.md"
    - from: "01_physics/05_noise_analysis.md"
      to: "00_notation/parameter_table.md"
      via: "References component parameters (NF values TBD)"
      pattern: "parameter_table\\.md"
---

<objective>
Write the noise analysis document covering cascaded noise figure from Friis formula through the analog chain, ADC quantization noise, and CIC filter digital noise contribution.

Purpose: This document bridges the radar equation SNR to actual system performance by tracing noise through every stage of the receive chain, including the often-overlooked digital processing stages.
Output: 01_physics/05_noise_analysis.md
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-physics-foundation/02-RESEARCH.md
@00_notation/conventions.md
@00_notation/symbol_table.md
@00_notation/parameter_table.md
@01_physics/01_fmcw_theory.md

<interfaces>
<!-- From Plan 02-01: FMCW theory provides radar equation SNR -->
Key equation from 01_fmcw_theory.md:
- Radar range equation with noise figure F and noise bandwidth B_n
- System noise figure F appears in the denominator of SNR

Key parameters from parameter_table.md:
- LNA: ADTR1107 (NF TBD)
- Mixer: LT5552
- ADC: AD9484, 8-bit, 400 MSPS
- CIC: 5 stages, decimation factor 4
- IF frequency: 120 MHz
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write noise analysis document</name>
  <files>01_physics/05_noise_analysis.md</files>
  <action>
Create `01_physics/05_noise_analysis.md` covering PHYS-06. Use equation prefix NF with tags NF-1 through approximately NF-12.

**Document structure (from research):**

1. **Thermal noise power** -- $P_n = k_B T_0 B_n$. Define noise temperature $T_0 = 290~\text{K}$ (standard reference). Noise bandwidth $B_n$ and its relationship to system bandwidth. This is the noise floor against which signals are detected.

2. **Noise figure definition** -- $F = \text{SNR}_\text{in} / \text{SNR}_\text{out}$ (linear ratio). Relationship to equivalent noise temperature: $T_e = T_0(F-1)$. Noise figure in dB: $\text{NF}_\text{dB} = 10\log_{10}(F)$. Physical interpretation: F measures how much a component degrades SNR.

3. **Friis cascaded noise figure** -- Derive the general M-stage formula: $F_\text{sys} = F_1 + (F_2-1)/G_1 + (F_3-1)/(G_1 G_2) + \ldots$. Per research "Don't Hand-Roll": use the standard Friis formula. Per research Pitfall 2: EXPLICITLY show the dB-to-linear conversion step. Work the entire cascade in LINEAR values. State clearly: "All $F$ and $G$ values in this formula are linear ratios, NOT dB." Include a conversion reminder box.

4. **AERIS-10 receive chain stages** -- Enumerate the stages symbolically:
   - Stage 1: LNA (ADTR1107) -- noise figure $F_\text{LNA}$, gain $G_\text{LNA}$
   - Stage 2: Mixer (LT5552) -- noise figure $F_\text{mix}$, conversion gain $G_\text{mix}$
   - Stage 3: IF amplifier (if present) -- $F_\text{IF}$, $G_\text{IF}$
   - Stage 4: ADC (AD9484) -- noise figure $F_\text{ADC}$, "gain" $G_\text{ADC}$
   Write the Friis formula for this specific chain. Keep all NF/gain values as symbols referencing parameter_table.md. Note that $F_\text{LNA}$ and several other values are TBD.

5. **ADC noise contribution** -- This is CRITICAL for the 8-bit AD9484. Derive quantization noise power: $\sigma_q^2 = \Delta^2/12$ where $\Delta = V_\text{FS}/2^b$ is the LSB voltage. Effective noise figure of the ADC: derive from the ratio of total output noise (thermal + quantization) to input noise. For an 8-bit ADC, the quantization noise floor is $-6.02 \times 8 - 1.76 = -49.9~\text{dBFS}$ (reference Open Question 3 from research). This is significantly higher than a 14-bit ADC and may dominate the digital noise floor. Show that the ADC noise figure depends on the signal level relative to full scale.

6. **CIC filter noise analysis** -- Per research Pitfall 4: do NOT stop the noise chain at the ADC. The CIC decimator with $N_\text{CIC} = 5$ stages and decimation factor $D_\text{CIC} = 4$ has:
   - Ideal processing gain: $D_\text{CIC}^{N_\text{CIC}} = 4^5 = 1024$ (30.1 dB)
   - But bit growth through CIC stages requires truncation/rounding, which adds quantization noise
   - Passband droop of the CIC filter affects effective SNR at band edges
   Derive the effective noise figure contribution of the digital processing chain. Show the bit growth formula for CIC: output width = input width + $N_\text{CIC} \lceil\log_2(D_\text{CIC})\rceil$ bits.

7. **System noise figure budget** -- Complete chain from antenna to digital output. Tabular format showing each stage's $F$, $G$, cumulative $F_\text{sys}$. Keep values symbolic. Include a subsection titled "Numerical Evaluation (Pending Parameter Resolution)" that shows the substitution procedure: "Once $F_\text{LNA}$ is resolved from the ADTR1107 datasheet, substitute into Eq. (NF-N) as follows..." Use representative placeholder values CLEARLY LABELED per research recommendation: "Using representative values $F_\text{LNA} = 3~\text{dB}$ (to be confirmed against component datasheets)..."

**Formatting:** Follow conventions.md. Symbolic derivations. Cross-reference FMCW theory for radar equation. Reference parameter_table.md for all component values (noting TBDs). Conversion reminder for dB vs linear.
  </action>
  <verify>
    <automated>test -f 01_physics/05_noise_analysis.md && grep -c "\\\\tag{NF-" 01_physics/05_noise_analysis.md | xargs -I{} test {} -ge 8 && grep -q "Friis" 01_physics/05_noise_analysis.md && grep -q "quantization" 01_physics/05_noise_analysis.md && grep -q "CIC" 01_physics/05_noise_analysis.md && grep -q "symbol_table" 01_physics/05_noise_analysis.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Noise analysis document exists with 8+ tagged equations covering thermal noise, noise figure definition, Friis cascade (with explicit dB/linear conversion), ADC quantization noise (8-bit specific), CIC filter noise growth, and complete system noise budget with pending numerical evaluation section</done>
</task>

</tasks>

<verification>
- Document follows conventions.md with NF-N tags
- Friis formula uses LINEAR values with explicit conversion reminder
- ADC quantization noise is derived for 8-bit specifically (not generic)
- CIC filter noise is included (chain does NOT stop at ADC)
- Numerical evaluation section uses clearly labeled placeholder values
- All component values reference parameter_table.md
- No hardcoded numerical values in derivation equations
</verification>

<success_criteria>
- Document traces noise from antenna through digital output without gaps
- An engineer can substitute real component values once TBDs are resolved
- The 8-bit ADC quantization noise is properly contextualized as a significant contributor
- CIC filter noise growth is quantified, not hand-waved
</success_criteria>

<output>
After completion, create `.planning/phases/02-physics-foundation/02-04-SUMMARY.md`
</output>
