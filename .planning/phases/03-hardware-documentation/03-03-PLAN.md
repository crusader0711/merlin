---
phase: 03-hardware-documentation
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - 02_hardware/04_antenna_beamforming.md
  - 02_hardware/05_fpga_board.md
autonomous: true
requirements: [HDWR-04, HDWR-05]

must_haves:
  truths:
    - "ADAR1000 beamformer documentation covers register map, phase/gain control, beam steering tables, and SPI interface"
    - "16-element array geometry and 31 elevation beam positions are documented with phase_differences mapping"
    - "FPGA board document covers all clock domains, CDC synchronizers, and module inventory"
    - "FPGA resource capacity (XC7A100T) is documented even if actual utilization is unavailable"
  artifacts:
    - path: "02_hardware/04_antenna_beamforming.md"
      provides: "ADAR1000 beamformer hardware docs with register map and beam steering tables"
      contains: "\\tag{HW-ANT"
    - path: "02_hardware/05_fpga_board.md"
      provides: "FPGA board documentation with clock domains, CDC, module inventory"
      contains: "\\tag{HW-FPGA"
  key_links:
    - from: "02_hardware/04_antenna_beamforming.md"
      to: "01_physics/03_beamforming_theory.md"
      via: "Cross-reference to array factor derivation"
      pattern: "BF-"
    - from: "02_hardware/05_fpga_board.md"
      to: "02_hardware/03_frequency_synthesis.md"
      via: "Clock source references"
      pattern: "03_frequency_synthesis"
---

<objective>
Document the antenna/beamforming subsystem (ADAR1000, 16-element array) and FPGA board (XC7A100T clock domains, CDC, resources).

Purpose: HDWR-04 bridges Phase 2 beamforming theory to actual hardware implementation (ADAR1000 register-level control, beam steering tables from firmware). HDWR-05 documents the digital processing platform that all signal processing runs on. These are independent of the RF chain docs and can execute in parallel with Plan 02.

Output: `02_hardware/04_antenna_beamforming.md` and `02_hardware/05_fpga_board.md`
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardware-documentation/03-RESEARCH.md
@00_notation/conventions.md
@00_notation/symbol_table.md
@00_notation/parameter_table.md

Source files to read during execution:
- 9_Firmware/9_1_Microcontroller/9_1_1_C_Cpp_Libraries/adar1000.h (ADAR1000 register map)
- 9_Firmware/9_1_Microcontroller/9_1_1_C_Cpp_Libraries/ADAR1000_Manager.h (beamformer API)
- 9_Firmware/9_1_Microcontroller/9_1_1_C_Cpp_Libraries/ADAR1000_Manager.cpp (beam matrix init)
- 9_Firmware/9_1_Microcontroller/9_1_3_C_Cpp_Code/main.cpp (phase_differences array, beam matrices, lines ~238-435)
- 9_Firmware/9_2_FPGA/radar_system_top.v (top-level FPGA with clock domains)
- 9_Firmware/9_2_FPGA/cdc_modules.v (CDC synchronizers)
- 9_Firmware/9_2_FPGA/cntrt.xdc (constraints -- pin assignments, clock constraints)
- 01_physics/03_beamforming_theory.md (array factor equations to cross-reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document antenna array and beamforming hardware</name>
  <files>02_hardware/04_antenna_beamforming.md</files>
  <action>
Create `02_hardware/04_antenna_beamforming.md` with `HW-ANT-` equation prefix following conventions.md template.

Structure:
1. **Overview** -- 16-element linear array with 4x ADAR1000 beamformer ICs (4 channels each). STM32 controls beamformers via SPI1. Two antenna variants per conventions.

2. **ADAR1000 Beamformer IC** --
   - Function: 4-channel analog beamformer with independent phase/gain per channel, TX/RX modes
   - SPI interface: extract address width, data width, R/W bit from `adar1000.h`
   - Key register map: extract from `adar1000.h` -- phase/gain registers, TX/RX control, T/R switch, calibration registers. Present per Pattern 2 from research.
   - Phase control: 7-bit resolution (2.8125 deg/step), 128 entries in VM lookup tables (VM_I, VM_Q, VM_GAIN)
   - Gain control: 128 levels via vector modulator
   - Fast switch mode for rapid TX/RX toggling
   - 4 ADAR1000 units: SPI chip select mapping to element groups

3. **Beam Steering Implementation** --
   - Document the `phase_differences[31]` array from main.cpp (all 31 values) with mapping to steering angles
   - Per-element phase computation: $\phi_n = n \times \Delta\phi_\text{pos}$ where $n$ is element index (0-15) and $\Delta\phi_\text{pos}$ is from `phase_differences[]` (HW-ANT equation)
   - Conversion to 7-bit ADAR1000 value: $\text{reg} = \lfloor (\phi_n / 360) \times 128 \rfloor$ (HW-ANT equation)
   - Beam matrices: document `matrix1[15][16]`, `matrix2[15][16]`, `vector_0[16]` structure from `initializeBeamMatrices()` in main.cpp
   - Cross-reference beamforming theory: Eq. (BF-*) from `01_physics/03_beamforming_theory.md` for array factor, grating lobe conditions

4. **16-Element Array Geometry** --
   - Physical layout: element spacing $d = \lambda/2 \approx 14.3~\text{mm}$
   - Variant callout: Nexus (8x16 patch array) vs Extended (32x16 slotted waveguide)
   - Grating lobe reference: cross-reference BF equations for $d/\lambda < 0.649$ safe condition

5. **References** -- symbol_table.md, parameter_table.md, ADAR1000 datasheet path, beamforming theory cross-refs.

Map firmware variables to standard symbols. Expected ~6-10 HW-ANT equations.
  </action>
  <verify>
    <automated>test -f 02_hardware/04_antenna_beamforming.md && grep -q "phase_differences" 02_hardware/04_antenna_beamforming.md && grep -c "\\\\tag{HW-ANT" 02_hardware/04_antenna_beamforming.md | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Antenna/beamforming document covers ADAR1000 register map, 31-position beam steering table with phase_differences array, per-element phase computation, beam matrix structure, variant callouts, and cross-references to beamforming theory.</done>
</task>

<task type="auto">
  <name>Task 2: Document FPGA board subsystem</name>
  <files>02_hardware/05_fpga_board.md</files>
  <action>
Create `02_hardware/05_fpga_board.md` with `HW-FPGA-` equation prefix following conventions.md template.

Structure:
1. **Overview** -- Xilinx Artix-7 XC7A100T as the central digital processing platform. Receives 8-bit samples at 400 MHz from AD9484, processes through DDC->CIC->matched filter->FFT->Doppler->CFAR pipeline, outputs via FT601 USB 3.0.

2. **XC7A100T Resource Capacity** --
   - Total resources: 63,400 LUTs, 126,800 FFs, 135 BRAMs (36Kb each), 240 DSP48E1 slices
   - Actual utilization: note as "requires Vivado implementation reports" per STATE.md blocker. If reports are unavailable, document theoretical per-module estimates.
   - BUFG usage: document from constraint file

3. **Clock Domains** --
   - 400 MHz ADC domain: captures LVDS data from AD9484 via `adc_dco_p/n`
   - 120 MHz DAC domain: `clk_120m_dac` for DAC output interface
   - 100 MHz system domain: `clk_100m` for processing pipeline
   - 100 MHz FT601 domain: `ft601_clk_in` external clock from FT601 IC
   - Document clock sources (all from AD9523, except FT601 which is external)
   - Pitfall reminder: use Pitfall 4 guidance from research to avoid clock name confusion

4. **CDC Synchronizers** --
   - Document from `cdc_modules.v`: Gray-coded multi-bit synchronizers, 3-stage metastability protection
   - ADC-to-processing CDC (400 MHz -> 100 MHz): module name, width, synchronizer stages
   - Other CDC crossings present in the design
   - HW-FPGA equations for metastability MTBF if derivable: $\text{MTBF} = \frac{1}{f_\text{clk} \cdot f_\text{data} \cdot T_w^2}$ (with appropriate constants)

5. **FPGA Module Inventory** --
   - Table listing all Verilog modules in `9_Firmware/9_2_FPGA/` with: module name, function, clock domain, key parameters
   - Modules from research: DDC (`ddc_400m.v`), CIC decimator (`cic_decimator.v`), matched filter (`matched_filter.v`), FFT (1024-pt forward+inverse), Doppler processor (`doppler_processor.v`, 32-pt FFT, 64 range bins), USB interface, DAC interface
   - This is a structural inventory only -- detailed signal processing pipeline documentation belongs in Phase 4 (SWDOC-01)

6. **Constraint File Summary** -- Extract key information from `cntrt.xdc`: clock definitions (period constraints), I/O standards (LVDS, LVCMOS), key pin assignments for ADC, DAC, FT601, and control signals.

7. **References** -- Cross-reference to frequency synthesis (clock sources), system overview (clock domain overview), and note that detailed FPGA signal processing is in Phase 4.

Expected ~5-8 HW-FPGA equations.
  </action>
  <verify>
    <automated>test -f 02_hardware/05_fpga_board.md && grep -q "XC7A100T" 02_hardware/05_fpga_board.md && grep -q "cdc" 02_hardware/05_fpga_board.md && grep -c "\\\\tag{HW-FPGA" 02_hardware/05_fpga_board.md | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>FPGA board document covers XC7A100T resource capacity, 4 clock domains with sources, CDC synchronizer implementation, complete Verilog module inventory, and constraint file summary. Detailed signal processing deferred to Phase 4.</done>
</task>

</tasks>

<verification>
- Both documents follow conventions.md template
- Antenna doc includes all 31 phase_differences values and steering angle mapping
- ADAR1000 register map extracted from adar1000.h (not manually transcribed)
- FPGA doc lists all clock domains with correct frequencies per Pitfall 4 guidance
- CDC synchronizer implementation documented from cdc_modules.v
- Verilog module inventory covers all modules in 9_Firmware/9_2_FPGA/
- Cross-references to Phase 2 beamforming theory and Phase 3 frequency synthesis
</verification>

<success_criteria>
- Engineer can configure beam steering by consulting the phase_differences table and ADAR1000 register map
- Engineer can identify which clock domain any FPGA module operates in
- CDC crossing points and synchronizer design are documented
- FPGA resource capacity vs actual utilization status is clear
</success_criteria>

<output>
After completion, create `.planning/phases/03-hardware-documentation/03-03-SUMMARY.md`
</output>
