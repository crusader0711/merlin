---
phase: 03-hardware-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 02_hardware/01_system_overview.md
  - 00_notation/symbol_table.md
autonomous: true
requirements: [HDWR-01]

must_haves:
  truths:
    - "System overview document exists with master block diagram description and subsystem cross-references"
    - "All hardware subsystem documents are listed and linked from the overview"
    - "Symbol table contains any new hardware-specific symbols added in this phase"
    - "Parameter table is referenced (not duplicated) for all numerical values"
  artifacts:
    - path: "02_hardware/01_system_overview.md"
      provides: "System overview with block diagram description, variant comparison, subsystem index"
      contains: "\\tag{HW-SYS"
    - path: "00_notation/symbol_table.md"
      provides: "Updated symbol table with any new hardware symbols"
  key_links:
    - from: "02_hardware/01_system_overview.md"
      to: "00_notation/parameter_table.md"
      via: "Markdown link references for all numerical values"
      pattern: "parameter_table.md"
    - from: "02_hardware/01_system_overview.md"
      to: "00_notation/symbol_table.md"
      via: "Symbol references on first use"
      pattern: "symbol_table.md"
---

<objective>
Create the system overview hardware document and update the symbol table with hardware-specific symbols.

Purpose: HDWR-01 is the foundation document that anchors all other hardware subsystem docs. It provides the master block diagram description, two-variant summary, and cross-references to every subsequent hardware document. Without it, other hardware docs lack their organizational anchor.

Output: `02_hardware/01_system_overview.md` with HW-SYS-tagged equations, plus symbol table updates.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardware-documentation/03-RESEARCH.md
@00_notation/conventions.md
@00_notation/symbol_table.md
@00_notation/parameter_table.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hardware-specific symbols to symbol table</name>
  <files>00_notation/symbol_table.md</files>
  <action>
Add a new section "7. Hardware and Power" to the symbol table with symbols needed for hardware documentation that are not already present. Key symbols to add:

- $V_\text{rail}$ -- Voltage rail value (V)
- $I_\text{rail}$ -- Current draw per rail (A)
- $P_\text{diss}$ -- Power dissipation (W)
- $T_\text{junction}$ -- Junction temperature (C or K)
- $\theta_{JA}$ -- Thermal resistance junction-to-ambient (C/W or K/W)
- $t_\text{lock}$ -- PLL lock time (s)
- $\mathcal{L}(f_m)$ -- Phase noise at offset $f_m$ (dBc/Hz)
- $t_\text{pipeline}$ -- End-to-end pipeline latency (s)
- $N_\text{LUT}$ -- FPGA LUT count (dimensionless)
- $N_\text{FF}$ -- FPGA flip-flop count (dimensionless)
- $N_\text{BRAM}$ -- FPGA Block RAM count (dimensionless)
- $N_\text{DSP}$ -- FPGA DSP48E1 slice count (dimensionless)

Follow the existing table format with Symbol, Definition, Units, IEEE 686 Ref columns. Check existing symbols to avoid duplicates -- several signal processing symbols already exist in Section 5.

Do NOT add symbols that already exist in any section (e.g., $F$, $G$, $f_s$, $f_\text{IF}$ are already defined).
  </action>
  <verify>
    <automated>grep -c "Hardware and Power" 00_notation/symbol_table.md | grep -q "1" && echo "PASS: Section exists" || echo "FAIL"</automated>
  </verify>
  <done>Symbol table has a new "Hardware and Power" section with ~12 hardware-specific symbols, no duplicates with existing sections</done>
</task>

<task type="auto">
  <name>Task 2: Create system overview document</name>
  <files>02_hardware/01_system_overview.md</files>
  <action>
Create `02_hardware/01_system_overview.md` following the document template from conventions.md with `HW-SYS-` equation prefix.

Structure:
1. **Overview** -- High-level system description of the AERIS-10 radar. Describe the functional block diagram (reference the existing `RADAR_V6.drawio` in `2_Functional Diagram & Interconnection Matrices/`). Describe the signal path: antenna elements -> ADAR1000 -> ADTR1107 -> LT5552 mixer -> AD9484 ADC -> FPGA (DDC->CIC->MF->FFT->CFAR) -> FT601 USB -> PC.

2. **System Parameter Summary** -- Do NOT duplicate parameter_table.md. Instead, create a concise "quick reference" table that lists the ~15 most important system parameters (fc, B, Tc1, Tc2, PRI, M, N, fs, N_FFT, etc.) with their symbols and a link to the parameter table for values. Include a two-variant comparison table for parameters that differ (Pt, antenna type, antenna gain, range).

3. **Subsystem Index** -- Table listing all 8 subsequent hardware documents (02 through 09) with:
   - Document filename and link
   - Subsystem name
   - Key components covered
   - Requirement ID

4. **Clock Domain Overview** -- Brief summary of the 4 clock domains (400 MHz ADC, 120 MHz DAC, 100 MHz system, 100 MHz FT601) referencing the AD9523 clock tree. Use an HW-SYS-tagged equation for the VCO frequency relationship: $f_\text{VCO} = f_\text{VCXO} \times N_\text{PLL2}$ where $N_\text{PLL2} = 36$, giving $f_\text{VCO} = 3.6~\text{GHz}$.

5. **Variant Comparison** -- Use the variant callout block format from conventions.md. Key differences: PA (ADTR1107 1W vs QPA2962 10W), antenna (8x16 patch vs 32x16 slotted waveguide), detection range (3 km vs 20 km).

6. **References** -- Links to symbol_table.md, parameter_table.md, conventions.md, and cross-references to Phase 2 physics docs (01_physics/*.md) where appropriate.

Use `\tag{HW-SYS-N}` for any equations. Expected ~3-5 equations (VCO frequency, clock divider relationships, system noise temperature reference).

Map firmware variables to standard symbols per conventions.md anti-pattern 5.4.
  </action>
  <verify>
    <automated>test -f 02_hardware/01_system_overview.md && grep -c "\\\\tag{HW-SYS" 02_hardware/01_system_overview.md | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>System overview document exists with block diagram description, parameter summary referencing parameter_table.md, subsystem index linking to all 8 subsequent docs, clock domain overview, and variant comparison. All equations use HW-SYS-N tags.</done>
</task>

</tasks>

<verification>
- `02_hardware/01_system_overview.md` exists and follows conventions.md template
- Symbol table has new hardware section with no duplicate symbols
- System overview references parameter_table.md (no inlined numerical values in derivations)
- All 8 subsequent hardware documents (02-09) are listed in the subsystem index
- Variant callout blocks used for Nexus vs Extended differences
</verification>

<success_criteria>
- System overview document is the organizational anchor for all hardware docs
- Engineers can find any hardware subsystem by consulting the subsystem index
- No numerical values are duplicated from parameter_table.md
- Symbol table is complete for hardware documentation needs
</success_criteria>

<output>
After completion, create `.planning/phases/03-hardware-documentation/03-01-SUMMARY.md`
</output>
