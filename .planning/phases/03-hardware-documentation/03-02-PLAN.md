---
phase: 03-hardware-documentation
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - 02_hardware/02_rf_frontend.md
  - 02_hardware/03_frequency_synthesis.md
autonomous: true
requirements: [HDWR-02, HDWR-03]

must_haves:
  truths:
    - "RF front-end document covers ADTR1107 LNA/PA, LT5552 mixer, and AD9484 ADC with key specs and SPI interface details"
    - "Frequency synthesis document covers AD9523 clock tree and ADF4382 TX/RX LO configuration with register maps"
    - "Clock tree table shows all 12 AD9523 outputs with frequencies, dividers, and destinations"
    - "ADF4382 TX and RX LO frequencies are documented with IF derivation"
  artifacts:
    - path: "02_hardware/02_rf_frontend.md"
      provides: "RF front-end documentation with ADTR1107, LT5552, AD9484 specs"
      contains: "\\tag{HW-RF"
    - path: "02_hardware/03_frequency_synthesis.md"
      provides: "Frequency synthesis documentation with AD9523 clock tree and ADF4382 LO config"
      contains: "\\tag{HW-FS"
  key_links:
    - from: "02_hardware/02_rf_frontend.md"
      to: "01_physics/05_noise_analysis.md"
      via: "Cross-reference to cascaded NF equations"
      pattern: "NF-"
    - from: "02_hardware/03_frequency_synthesis.md"
      to: "00_notation/parameter_table.md"
      via: "Frequency values reference"
      pattern: "parameter_table.md"
    - from: "02_hardware/02_rf_frontend.md"
      to: "02_hardware/03_frequency_synthesis.md"
      via: "LO frequency cross-reference for mixer documentation"
      pattern: "03_frequency_synthesis"
---

<objective>
Document the RF front-end chain (ADTR1107, LT5552, AD9484) and frequency synthesis subsystem (ADF4382, AD9523).

Purpose: HDWR-02 and HDWR-03 form the core analog RF documentation. The RF front-end defines the receive chain that determines system noise figure, while frequency synthesis defines the clock tree and LO generation that every other subsystem depends on. These are tightly coupled (mixer needs LO from synthesizer) and belong together.

Output: `02_hardware/02_rf_frontend.md` and `02_hardware/03_frequency_synthesis.md`
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardware-documentation/03-RESEARCH.md
@00_notation/conventions.md
@00_notation/symbol_table.md
@00_notation/parameter_table.md

Source files to read during execution:
- 9_Firmware/9_1_Microcontroller/9_1_1_C_Cpp_Libraries/adf4382.h (ADF4382 register map)
- 9_Firmware/9_1_Microcontroller/9_1_1_C_Cpp_Libraries/ad9523.h (AD9523 register map)
- 9_Firmware/9_1_Microcontroller/9_1_1_C_Cpp_Libraries/adf4382a_manager.h (LO frequencies)
- 9_Firmware/9_1_Microcontroller/9_1_3_C_Cpp_Code/main.cpp (configure_ad9523 function, lines ~924-1076)
- 9_Firmware/9_2_FPGA/ad9484_interface_400m.v (ADC LVDS interface)
- 9_Firmware/9_2_FPGA/lvds_to_cmos_400m.v (LVDS to CMOS conversion)
- 01_physics/05_noise_analysis.md (noise figure chain equations to cross-reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document RF front-end subsystem</name>
  <files>02_hardware/02_rf_frontend.md</files>
  <action>
Create `02_hardware/02_rf_frontend.md` with `HW-RF-` equation prefix following the conventions.md template.

Structure:
1. **Overview** -- RF receive chain signal path: antenna -> ADAR1000 (T/R switch) -> ADTR1107 (LNA in RX, PA in TX) -> LT5552 mixer -> AD9484 ADC. Describe the superheterodyne architecture with IF=120 MHz.

2. **ADTR1107 T/R Front-End Module** --
   - Function: integrated T/R module with LNA (receive) and PA (transmit) per element
   - Key specs from datasheet: gain, NF, IP3, P1dB (note NF is TBD in parameter_table.md -- document what is known, flag TBD)
   - TX/RX switching: controlled via ADAR1000 T/R pin
   - Power sequencing: document the ADTR1107 init from `ADAR1000_Manager::initializeADTR1107Sequence()` in the firmware
   - Variant callout: Nexus uses ADTR1107 (1W), Extended uses QPA2962 GaN (10W)

3. **LT5552 Mixer** --
   - Function: downconversion from RF to IF
   - Key specs: conversion loss, IP3, LO drive level
   - LO connection: fed by ADF4382 RX synthesizer at 10.38 GHz (cross-reference 03_frequency_synthesis.md)
   - IF output: $f_\text{IF} = f_\text{TX} - f_\text{RX} = 10.5 - 10.38 = 120~\text{MHz}$ (use HW-RF equation tag)

4. **AD9484 ADC** --
   - Specs: 8-bit, 500 MSPS rated, operated at 400 MSPS (from AD9523 OUT4/OUT5)
   - LVDS interface: document from `ad9484_interface_400m.v` and `lvds_to_cmos_400m.v` -- LVDS DDR data capture at 400 MHz, 8-bit parallel output
   - Clock source: 400 MHz from AD9523 (cross-reference 03_frequency_synthesis.md)
   - Pitfall reminder: This is 8-bit, NOT 14-bit (see parameter_table.md Inconsistency Resolution #3)
   - Dynamic range: $\text{ENOB} \approx 7.5$ bits typical, SFDR and SNR from datasheet

5. **Cascaded Noise Figure Reference** -- Do NOT re-derive. Reference Eq. (NF-*) from `01_physics/05_noise_analysis.md`. List the component order in the cascade (ADTR1107 -> LT5552 -> AD9484) and note which parameter values are TBD.

6. **References** -- symbol_table.md, parameter_table.md, datasheets (with paths to `7_Components Datasheets/` files), cross-references to noise analysis and frequency synthesis docs.

Map all firmware variable names to standard symbols per conventions.md anti-pattern 5.4.
  </action>
  <verify>
    <automated>test -f 02_hardware/02_rf_frontend.md && grep -c "\\\\tag{HW-RF" 02_hardware/02_rf_frontend.md | head -1 && grep -q "parameter_table" 02_hardware/02_rf_frontend.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>RF front-end document covers ADTR1107, LT5552, AD9484 with key specs, SPI/interface details, variant callouts, and cross-references to noise analysis and frequency synthesis. No inlined numerical values in derivations.</done>
</task>

<task type="auto">
  <name>Task 2: Document frequency synthesis subsystem</name>
  <files>02_hardware/03_frequency_synthesis.md</files>
  <action>
Create `02_hardware/03_frequency_synthesis.md` with `HW-FS-` equation prefix following conventions.md template.

Structure:
1. **Overview** -- Frequency synthesis architecture: 100 MHz VCXO -> AD9523-1 clock distributor (VCO=3.6 GHz) -> 12 output channels -> ADF4382 TX/RX synthesizers + ADC/DAC/FPGA clocks.

2. **AD9523-1 Clock Generator** --
   - VCO: $f_\text{VCO} = f_\text{VCXO} \times N_\text{PLL2} = 100~\text{MHz} \times 36 = 3.6~\text{GHz}$ (HW-FS equation)
   - Complete clock tree table (all 12 outputs) extracted from research doc: OUT0-OUT11 with frequency, format (LVDS/LVCMOS), divider ratio, destination, FPGA signal name
   - PLL2 configuration from firmware: `pll2_ndiv_b_cnt=9`, `pll2_r2_div=0` -> N=4*9+0=36
   - Key register map: extract the most important registers from `ad9523.h` -- PLL2 divider, output enables, output format, channel dividers. Present as structured table per Pattern 2 from research.
   - SPI interface: address width, data width, R/W bit position from the ad9523.h driver header
   - Lock detection and status registers
   - Firmware init: document the `configure_ad9523()` flow from main.cpp lines ~924-1076

3. **ADF4382 Synthesizers** --
   - Two instances: TX LO ($f_\text{TX} = 10.5~\text{GHz}$) and RX LO ($f_\text{RX} = 10.38~\text{GHz}$)
   - Reference clock: 300 MHz from AD9523 OUT0/OUT1
   - IF derivation equation: $f_\text{IF} = f_\text{TX} - f_\text{RX}$ (HW-FS equation, cross-ref to HW-RF)
   - Sync mechanism: 60 MHz sync clocks on AD9523 OUT8/OUT9 via SYNCP/SYNCN pins
   - Key register map: extract important registers from `adf4382.h` -- N divider, R divider, charge pump, lock detect. Structured table.
   - Lock detection: GPIO pins `ADF4382_TX_LKDET`, `ADF4382_RX_LKDET` from firmware
   - Phase noise: reference datasheet specs, note importance for Doppler floor
   - SPI configuration from `adf4382a_manager.h`

4. **OCXO Warm-Up Requirement** -- Document the 180-second warm-up delay (`HAL_Delay(180000)` at main.cpp:L1237) before clock configuration. This is Pitfall 3 from the research doc -- critical for timing documentation.

5. **References** -- Links to datasheets, symbol_table.md, parameter_table.md, cross-references to system overview clock domain section and RF front-end mixer LO.

Map firmware variables to standard symbols per conventions.md. Expected ~8-12 HW-FS equations.
  </action>
  <verify>
    <automated>test -f 02_hardware/03_frequency_synthesis.md && grep -c "\\\\tag{HW-FS" 02_hardware/03_frequency_synthesis.md | head -1 && grep -q "AD9523" 02_hardware/03_frequency_synthesis.md && grep -q "ADF4382" 02_hardware/03_frequency_synthesis.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Frequency synthesis document covers AD9523 complete clock tree (all 12 outputs), ADF4382 TX/RX LO configuration with register maps, OCXO warm-up requirement, lock detection, and cross-references to RF front-end and system overview. All equations use HW-FS-N tags.</done>
</task>

</tasks>

<verification>
- Both documents follow conventions.md template (Purpose, Prerequisites, numbered sections, References)
- RF front-end doc covers all 3 components (ADTR1107, LT5552, AD9484)
- Frequency synthesis doc includes complete AD9523 clock tree table (12 outputs)
- No numerical values inlined in derivations -- all reference parameter_table.md
- Variant callout blocks used where Nexus/Extended differ
- Firmware variables mapped to standard symbols throughout
- Cross-references between the two documents (mixer LO source)
</verification>

<success_criteria>
- Engineer can find RF chain specs (NF, gain, IP3) for each component
- Engineer can trace any clock frequency from VCXO through AD9523 divider to destination
- ADF4382 TX/RX LO frequencies and sync mechanism are fully documented
- OCXO warm-up requirement is documented (prevents Pitfall 3)
</success_criteria>

<output>
After completion, create `.planning/phases/03-hardware-documentation/03-02-SUMMARY.md`
</output>
