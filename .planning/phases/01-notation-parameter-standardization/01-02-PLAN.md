---
phase: 01-notation-parameter-standardization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 00_notation/parameter_table.md
autonomous: true
requirements:
  - NOTN-02

must_haves:
  truths:
    - "An engineer can look up any system parameter for either AERIS-10 variant in one canonical location"
    - "Every parameter value has a traceable source (firmware variable, FPGA parameter, datasheet)"
    - "Known inconsistencies between codebase sources are flagged and resolved or explicitly marked TBD"
    - "Codebase variable names are mapped to standard mathematical symbols"
  artifacts:
    - path: "00_notation/parameter_table.md"
      provides: "Master system parameter table with values for both Nexus and Extended variants"
      contains: "| Parameter | Symbol | Nexus"
  key_links:
    - from: "00_notation/parameter_table.md"
      to: "00_notation/symbol_table.md"
      via: "parameter_table uses symbols defined in symbol_table"
      pattern: "symbol_table"
    - from: "00_notation/parameter_table.md"
      to: "9_Firmware/9_1_Microcontroller/9_1_3_C_Cpp_Code/main.cpp"
      via: "firmware variable column traces values back to source code"
      pattern: "main\\.cpp"
---

<objective>
Create the master system parameter table with canonical numerical values for both AERIS-10 variants, mapping every parameter to its standard symbol and codebase source.

Purpose: Establish one canonical location for all system parameter values so no downstream document ever defines a numerical value inline. This eliminates the parameter drift failure mode where different documents cite different values for the same parameter.
Output: `00_notation/parameter_table.md` committed to repository root.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-notation-parameter-standardization/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create master system parameter table</name>
  <files>00_notation/parameter_table.md</files>
  <action>
Create `00_notation/parameter_table.md` with the following structure:

**Header:** Title ("Master System Parameter Table -- AERIS-10"), purpose statement ("Single source of truth for all numerical parameter values"), links to `symbol_table.md` for symbol definitions and `conventions.md` for formatting rules.

**Inconsistency Resolution Section** -- Address the 4 inconsistencies found in research BEFORE the main table, so readers understand the canonical choices:

1. **Center frequency:** Canonical value is $f_c = 10.5~\text{GHz}$ (matches README, firmware wavelength constant $\lambda = 0.02857$ m). Note: GUI `system_frequency` defaults to $10.0~\text{GHz}$ -- this appears to be an outdated default. Flag for codebase correction.

2. **PRF discrepancy:** `PRI1 = 167~\mu\text{s}` in firmware ($f_{r,1} \approx 5988~\text{Hz}$) is the chirp-level repetition interval. GUI `prf1 = 1000~\text{Hz}` likely refers to a display/processing update rate, NOT the chirp PRF. Document both with clear labels.

3. **ADC resolution:** AD9484 is an 8-bit 500 MSPS ADC (per datasheet). STACK.md claim of "14-bit" is incorrect -- likely a confusion with a different ADC or a planned upgrade. Canonical: 8-bit.

4. **Beam steering range:** `phase_differences[31]` array covers inter-element phase shifts from $-160^\circ$ to $+160^\circ$, which maps to elevation steering angles via $\theta = \arcsin(\Delta\phi \cdot \lambda / (2\pi d))$.

**Main Parameter Table** -- Use this column structure:
`| Parameter | Symbol | Nexus (AERIS-10N) | Extended (AERIS-10X) | Firmware Var | FPGA Param | GUI Var | Source |`

Organize rows by subsystem. Populate ALL values from the research codebase audit:

**Waveform and Timing:**
- Center frequency: $f_c$ | 10.5 GHz | 10.5 GHz | -- | -- | `system_frequency` | README, firmware $\lambda$
- Chirp bandwidth: $B$ | TBD | TBD | -- | -- | -- | Requires datasheet
- Long chirp duration: $T_{c,1}$ | 30 us | 30 us | `T1` | `USE_LONG_CHIRP` | `chirp_duration_1` | main.cpp:L~200
- Short chirp duration: $T_{c,2}$ | 0.5 us | 0.5 us | `T2` | -- | `chirp_duration_2` | main.cpp:L~201
- Long chirp PRI: $T_{r,1}$ | 167 us | 167 us | `PRI1` | -- | -- | main.cpp:L~202
- Short chirp PRI: $T_{r,2}$ | 175 us | 175 us | `PRI2` | -- | -- | main.cpp:L~203
- Guard time: $T_\text{guard}$ | 175.4 us | 175.4 us | `Guard` | -- | -- | main.cpp:L~204
- Chirps per position: $M$ | 32 | 32 | `m_max` | `CHIRPS_PER_FRAME` | `chirps_per_position` | main.cpp, doppler_processor.v
- Elevation positions: $N_\text{el}$ | 31 | 31 | `n_max` | -- | -- | main.cpp
- Azimuth positions: $N_\text{az}$ | 50 | 50 | `y_max` | -- | -- | main.cpp
- Wavelength: $\lambda$ | 0.02857 m | 0.02857 m | `wavelength` | -- | -- | main.cpp:L1133

**Antenna and Beamforming:**
- Array elements: $N$ | 16 (8x2 subarrays) | 16 (cascaded) | -- | -- | -- | README
- Element spacing: $d$ | $\lambda/2$ (~14.3 mm) | $\lambda/2$ | `element_spacing` | -- | -- | main.cpp:L1134
- Antenna type: -- | 8x16 patch array | 32x16 slotted waveguide | -- | -- | -- | README
- Phase shift range: $\Delta\phi_n$ | -160 to +160 deg | -160 to +160 deg | `phase_differences[31]` | -- | -- | main.cpp
- ADAR1000 units: -- | 4 | 4 | -- | -- | -- | README

**RF Front-End:**
- Output power/element: $P_t$ | ~1 W (ADTR1107) | 10 W (QPA2962 GaN) | -- | -- | -- | README
- LNA noise figure: $F_\text{LNA}$ | TBD (ADTR1107 datasheet) | TBD | -- | -- | -- | Datasheet needed
- Mixer: -- | LT5552 | LT5552 | -- | -- | -- | README
- ADC: -- | AD9484 (8-bit, 500 MSPS) | AD9484 (8-bit, 500 MSPS) | -- | -- | -- | Datasheet

**Signal Processing (FPGA):**
- ADC sample rate: $f_s$ | 400 MHz | 400 MHz | -- | `FS` | -- | ddc_400m.v
- IF frequency: $f_\text{IF}$ | 120 MHz | 120 MHz | `IF_freq` | `IF_FREQ` | -- | main.cpp, ddc_400m.v
- ADC data width: -- | 8 bits | 8 bits | -- | `ADC_WIDTH` | -- | ddc_400m.v
- NCO width: -- | 16 bits | 16 bits | -- | `NCO_WIDTH` | -- | ddc_400m.v
- CIC stages: $N_\text{CIC}$ | 5 | 5 | -- | `STAGES` | -- | cic_decimator.v
- CIC decimation: $D_\text{CIC}$ | 4 | 4 | -- | `DECIMATION` | -- | cic_decimator.v
- FFT/buffer size: $N_\text{FFT}$ | 1024 | 1024 | -- | `BUFFER_SIZE` | -- | matched_filter.v
- Doppler FFT size: $N_\text{Doppler}$ | 32 | 32 | -- | `DOPPLER_FFT_SIZE` | -- | doppler_processor.v
- Range bins: $N_R$ | 64 | 64 | -- | `RANGE_BINS` | -- | doppler_processor.v
- Long chirp samples: -- | 3000 | 3000 | -- | `LONG_CHIRP_SAMPLES` | -- | matched_filter.v
- Short chirp samples: -- | 50 | 50 | -- | `SHORT_CHIRP_SAMPLES` | -- | matched_filter.v

**Frequency Synthesis:**
- Clock reference: -- | 100 MHz (AD9523-1) | 100 MHz (AD9523-1) | -- | -- | -- | README
- Synthesizers: -- | 2x ADF4382 (TX/RX LO) | 2x ADF4382 | -- | -- | -- | README

**System-Level (Derived/TBD):**
- Max detection range: $R_\text{max}$ | 3 km | 20 km | -- | -- | `max_distance` (50 km display) | README
- Stepper steps/rev: -- | 200 | 200 | `Stepper_steps` | -- | -- | main.cpp
- Magnetic declination: -- | -0.61 deg | -0.61 deg | `Mag_Declination` | -- | -- | main.cpp
- GUI PRF long: -- | -- | -- | -- | -- | `prf1` = 1000 Hz | GUI_V6.py (display rate, NOT chirp PRF)
- GUI PRF short: -- | -- | -- | -- | -- | `prf2` = 2000 Hz | GUI_V6.py (display rate)

**FPGA Clock Domains:**
- ADC clock: -- | 400 MHz | 400 MHz | -- | -- | -- | ARCHITECTURE.md
- DAC clock: -- | 120 MHz | 120 MHz | -- | `clk_120m_dac` | -- | dac_interface_single.v
- System clock: -- | 100 MHz | 100 MHz | -- | -- | -- | ARCHITECTURE.md

**TBD Tracking Section** at the bottom -- list all TBD values with what is needed to resolve them:
- $B$ (chirp bandwidth): Requires ADF4382 configuration analysis or chirp LUT examination
- $F_\text{LNA}$ (ADTR1107 noise figure): Requires ADTR1107 datasheet lookup
- Extended variant antenna gain: Requires slotted waveguide array analysis
- Extended variant noise figure chain: Requires QPA2962 datasheet

Mark each TBD as "Required before Phase 2" or "Required before Phase 3" based on which downstream phase needs it.

**Footer:** "All parameter values must be updated in THIS file only. No other document may define canonical numerical values. If a discrepancy is found between this table and a source, update this table and note the resolution."
  </action>
  <verify>
    <automated>test -f 00_notation/parameter_table.md && grep -q 'Nexus' 00_notation/parameter_table.md && grep -q 'Extended' 00_notation/parameter_table.md && grep -q 'TBD' 00_notation/parameter_table.md && grep -q '10.5' 00_notation/parameter_table.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>parameter_table.md exists with: (1) Inconsistency resolution section addressing all 4 research-identified conflicts, (2) Main table with 30+ parameters covering waveform, antenna, RF, FPGA, frequency synthesis, and system-level domains, (3) Both Nexus and Extended variant columns populated, (4) Firmware/FPGA/GUI variable mapping columns, (5) TBD tracking section with downstream phase dependencies. An engineer can look up any system parameter without searching multiple files.</done>
</task>

</tasks>

<verification>
1. `00_notation/parameter_table.md` has separate columns for Nexus and Extended variants
2. All 4 inconsistencies from research are addressed with canonical resolutions
3. Firmware variables (`T1`, `PRI1`, `m_max`, etc.) are mapped to standard symbols
4. FPGA parameters (`IF_FREQ`, `FS`, `CHIRPS_PER_FRAME`, etc.) are mapped to standard symbols
5. GUI variables (`system_frequency`, `chirp_duration_1`, etc.) are mapped to standard symbols
6. TBD values are flagged with which downstream phase requires them
7. Center frequency is canonically 10.5 GHz
8. ADC is canonically 8-bit (AD9484)
</verification>

<success_criteria>
- parameter_table.md contains 30+ parameters across all subsystem domains
- Both AERIS-10 variants have dedicated columns with values populated where known
- Every parameter has traceability to its source (firmware line, FPGA file, datasheet)
- All 4 codebase inconsistencies are resolved with documented rationale
- TBD items are tracked with phase dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-notation-parameter-standardization/01-02-SUMMARY.md`
</output>
