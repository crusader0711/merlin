---
phase: 04-software-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 03_software/02_stm32_firmware.md
autonomous: true
requirements:
  - SWDOC-02

must_haves:
  truths:
    - "Engineer can follow the complete 17-step initialization sequence with exact timings and GPIO pins"
    - "Every SPI/I2C device address and bus assignment is documented in tables"
    - "All magic numbers (PRI1=167us, Guard=175.4us, phase_differences[31], DAC_val=126, Idq=1.680A) are derived from physics or component specs"
    - "The main radar loop (chirp sequencing, beam steering, data collection) is documented as a control flow"
  artifacts:
    - path: "03_software/02_stm32_firmware.md"
      provides: "STM32 firmware documentation covering init, peripherals, and radar loop"
      min_lines: 300
      contains: "SW-"
  key_links:
    - from: "03_software/02_stm32_firmware.md"
      to: "02_hardware/06_power_management.md"
      via: "Cross-reference for power-on sequence and GPIO rail control"
      pattern: "power_management"
    - from: "03_software/02_stm32_firmware.md"
      to: "02_hardware/04_antenna_beamforming.md"
      via: "Cross-reference for ADAR1000 beam steering and phase_differences derivation"
      pattern: "antenna_beamforming"
    - from: "03_software/02_stm32_firmware.md"
      to: "01_physics/01_fmcw_theory.md"
      via: "Cross-reference for PRI and chirp timing derivations"
      pattern: "FMCW-"
---

<objective>
Document the STM32 firmware (main.cpp) covering the 17-step initialization sequence, SPI/I2C peripheral configuration, magic number derivations, and main radar loop control flow.

Purpose: Engineers can understand every firmware initialization step, locate any peripheral's I2C/SPI address, and derive every magic number from first principles or component specifications.
Output: `03_software/02_stm32_firmware.md`
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-software-documentation/04-RESEARCH.md

Source files to read:
@9_Firmware/9_1_Microcontroller/9_1_3_C_Cpp_Code/main.cpp
@9_Firmware/9_1_Microcontroller/9_1_3_C_Cpp_Code/main.h

Existing docs for cross-reference:
@00_notation/conventions.md
@00_notation/symbol_table.md
@02_hardware/01_system_overview.md
@02_hardware/02_rf_frontend.md
@02_hardware/03_frequency_synthesis.md
@02_hardware/04_antenna_beamforming.md
@02_hardware/06_power_management.md
@02_hardware/08_power_budget.md
@02_hardware/09_gps_imu_transforms.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write STM32 firmware documentation</name>
  <files>03_software/02_stm32_firmware.md</files>
  <action>
Create `03_software/02_stm32_firmware.md` following conventions.md document template. Ensure `03_software/` directory exists (create if Plan 01 has not run yet).

**Document structure:**

1. **Header** -- Purpose, prerequisites, scope (main.cpp ~2000 lines, main.h GPIO pin map)

2. **Firmware Architecture Overview** -- Three phases: initialization (steps 1-17), main radar loop, and error handling. Brief block diagram as table.

3. **Initialization Sequence** -- Full 17-step table from research Pattern 2 (reproduced here for executor convenience):

| Step | Action | Code Location | Duration |
|------|--------|---------------|----------|
| 1 | MPU/HAL/Clock config | MPU_Config(), HAL_Init(), SystemClock_Config() | ~ms |
| 2 | Peripheral init | MX_GPIO_Init() through MX_USB_DEVICE_Init() | ~ms |
| 3 | Start TIM1 + DWT | HAL_TIM_Base_Start(), DWT_Init() | ~ms |
| 4 | OCXO warm-up wait | HAL_Delay(180000) | 180 seconds |
| 5 | AD9523 power sequence | 1V8_CLOCK -> 3V3_CLOCK -> release reset | 300 ms |
| 6 | AD9523 clock configuration | configure_ad9523() -- 12 channel outputs | ~100 ms |
| 7 | FPGA power sequence | 1V0 -> 1V8 -> 3V3 (100ms each) | 300 ms |
| 8 | IMU initialization | GY85_Init() + 10 iterations with complementary filter | ~3 s |
| 9 | Barometer calibration | myBMP.getPressure() x 5 iterations | ~500 ms |
| 10 | ADF4382 LO initialization | ADF4382A_Manager_Init() + lock wait (up to 10s) | 1-10 s |
| 11 | ADAR1000 power + init | 3V3_ADAR12/34 -> 5V0_ADAR -> systemPowerUpSequence() | ~1 s |
| 12 | Beam matrix initialization | initializeBeamMatrices() -- 31 positions x 16 elements | ~ms |
| 13 | GPS acquisition | smartDelay(1000) x 10 iterations | 10 s |
| 14 | Point stepper to North | Stepper motor rotation based on IMU yaw | variable |
| 15 | Send GPS to GUI + wait for start | GPS_SendBinaryToGUI() + poll isStartFlagReceived() | user-dependent |
| 16 | PA power-up (if enabled) | DAC5578 init -> set Vg -> enable VDD -> Idq tuning | ~2 s |
| 17 | FPGA reset + enable mixers | GPIO toggle PD12 + PD11 | ~10 ms |

For each step, provide a paragraph of detail: which GPIO pins are toggled, what the function does, why the delay exists (e.g., OCXO crystal warm-up for frequency stability), and cross-references to relevant Phase 3 hardware docs.

Cross-reference: Steps 5-7 -> `02_hardware/06_power_management.md`, Step 6 -> `02_hardware/03_frequency_synthesis.md`, Step 10 -> `02_hardware/03_frequency_synthesis.md`, Steps 11-12 -> `02_hardware/04_antenna_beamforming.md`.

4. **Peripheral Configuration Tables** -- I2C device address table (from research: DAC5578 0x48/0x49, ADS7830 0x48/0x4A/0x49, GY-85, BMP180) and SPI device table (ADAR1000 SPI1, AD9523 SPI4, ADF4382 TX/RX SPI4) exactly as extracted in research. Include CS pin names from main.h.

5. **Magic Number Derivations** -- For each magic number, derive from physics or component specs using SW-tagged equations:

- PRI1 = 167 us: $T_{r,1} = T_{c,1} + t_\text{processing} = 30 + 137 = 167~\mu\text{s}$ \tag{SW-N}
- Guard = 175.4 us: Guard time derivation between long and short chirp sequences
- phase_differences[31]: $\Delta\phi_n$ for 31 elevation beam positions. Derive from $\Delta\phi = \frac{2\pi d \sin\theta}{\lambda}$ with $d = \lambda/2$. Cross-reference Eq. (BF-series) in `01_physics/03_beamforming_theory.md`.
- DAC_val = 126: PA gate voltage derivation. Opamp output Vg = -3.98V, input = 1.63058V, DAC code = $\frac{1.63}{3.3} \times 255 \approx 126$
- Idq target = 1.680A: QPA2962 quiescent drain current target
- Stepper_steps = 4: 200 steps/rev / 50 azimuth positions = 4 steps per position

Per conventions.md anti-pattern 5.4: ALWAYS pair code variable names with standard symbols. E.g., "The long chirp PRI $T_{r,1}$ (`PRI1` in main.cpp)".

6. **Main Radar Loop** -- Document the control flow after initialization:
- Azimuth sweep: 50 positions, stepper motor steps between each
- Elevation sweep: 31 beam positions per azimuth, ADAR1000 phase updates via SPI
- Chirp sequencing: long chirp (30 us) then short chirp (0.5 us) per beam position
- FPGA handshake: `stm32_new_chirp` (PD8), `stm32_new_elevation` (PD9), `stm32_new_azimuth` (PD10) edge signals
- Data collection: USB transfer coordination, GPS/IMU data packaging

7. **GPIO Pin Map** -- Complete table from main.h showing all GPIO pins and their functions, grouped by subsystem (power control, SPI CS, FPGA control, sensors, stepper, indicators).

8. **Error Handling** -- Any error detection, watchdog, or recovery mechanisms in main.cpp.

9. **References** -- Symbol table, parameter table, Phase 2 physics, Phase 3 hardware docs.

**Equation numbering:** Continue SW-N tags from where Plan 01 leaves off. If Plan 01 has not been executed yet, start at SW-20 to leave room for FPGA pipeline equations (research indicates ~10-15 equations for the pipeline). If Plan 01 summary exists, read it to determine the last SW-N tag used.
  </action>
  <verify>
    <automated>test -f 03_software/02_stm32_firmware.md && grep -c "\\\\tag{SW-" 03_software/02_stm32_firmware.md | xargs test 3 -le</automated>
  </verify>
  <done>STM32 firmware document exists with: 17-step initialization sequence table with timings, I2C/SPI device address tables, all 6+ magic number derivations with SW-tagged equations, main radar loop control flow, GPIO pin map, and cross-references to Phase 2/3 docs. Every firmware variable name is paired with its standard symbol.</done>
</task>

</tasks>

<verification>
- `03_software/02_stm32_firmware.md` exists and follows conventions.md template
- 17-step init sequence is complete with GPIO pins and timings
- I2C device address table matches research (DAC5578, ADS7830, GY-85, BMP180)
- SPI device table matches research (ADAR1000, AD9523, ADF4382)
- All magic numbers have derivation equations
- Main radar loop documents azimuth/elevation/chirp sequencing
- GPIO pin map is complete
- Every code variable is paired with standard symbol (anti-pattern 5.4)
- Cross-references to Phase 2/3 docs are present
</verification>

<success_criteria>
An engineer reading 02_stm32_firmware.md can reproduce the complete power-up and initialization sequence, locate any peripheral's bus address and configuration, derive every magic number from first principles, and understand the main radar loop's control flow for azimuth/elevation scanning with chirp sequencing.
</success_criteria>

<output>
After completion, create `.planning/phases/04-software-documentation/04-02-SUMMARY.md`
</output>
